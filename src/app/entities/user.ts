import { Role } from "./role";

export class User {
    id: number;
    userName: string;
    fullName: string;
    email: string;
    password: string;
    phoneNumber: string;
    roles: String[];
}